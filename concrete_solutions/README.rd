Teste da Concrete Solutions, um teste para aplicar os descontos em produtos, os itens A, B, C são produtos diferentes, e o desconto é aplicado conforme a regra que está no site deles.

- A ideia foi fazer o teste em poucas linhas (34 linhas), ou seja tenho uma função que aplica os valores em desconto e outra que vai aplicar os descontos.
- Não fiz os testes unitários sem importar o unittest, geralmente os rubystas fazem com o Rspec, não com o unittest, como são testes com a mesma função não vi necessidade de importar uma biblioteca para isso, com uma simples função podemos fazer isso.
- Fiz um módulo chamado de DataBase, ele vai tratar as informações que preciso para realizar os testes.
